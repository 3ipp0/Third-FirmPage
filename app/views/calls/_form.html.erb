                        <%= form_for @call,:url => url_for(:controller => 'calls', :action => 'create'), method: :post, remote: true do |f| %>
                            <% if @call.errors.any? %>
                                <div id="error_explanation">
                                    <h2><%= pluralize(@call.errors.count, "error") %> prohibited this Call from being saved:</h2>
                                    <ul>
                                    <% @call.errors.full_messages.each do |msg| %>
                                        <div class="alert alert-danger"><%= msg %></div>
                                    <% end %>
                                    </ul>
                                </div>
                            <% end %>

                            <div class="field">
                                <%= f.label :full_name %><BR>
                                <%= f.text_field :full_name %>
                            </div>

                            <div class="field">
                                <%= f.label :phone_number %><BR>
                                <%= f.text_field :phone_number %>
                            </div>

                            <div class="field">
                                <%= f.label "Addictional information, if needed" %><BR>
                                <%= f.text_field :body %>
                            </div>

                            <div class="actions">
                                <%= f.submit %>
                            </div>
                        <% end %>